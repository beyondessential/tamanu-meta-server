pre-release-hook = ["git", "cliff", "-c", "../../cliff.toml", "-o", "../../CHANGELOG.md", "--tag", "{{version}}"]
pre-release-replacements = [
  { file = "../../README.md", search = "canopy:[0-9\\.]+", replace = "canopy:{{version}}"},
]
