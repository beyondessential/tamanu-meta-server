<!DOCTYPE html>
<html data-theme="light">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/svg+xml" href="/static/images/favicon.svg" />
    <link rel="stylesheet" href="/static/bulma/bulma.min.css">
    <link rel="stylesheet" href="/static/private/main.css">
    <title>Tamanu {{ version.major }}.{{ version.minor }}.{{ version.patch }} — Mobile Install</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .container {
            flex: 1;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
        }

        .version-badge {
            display: inline-block;
            margin-bottom: 2rem;
        }

        .platform-card {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            padding: 1.5rem;
            margin-bottom: 1rem;
        }

        .platform-icon {
            display: flex;
            align-items: center;
            justify-content: center;
            min-width: 48px;
        }

        .platform-icon svg {
            width: 48px;
            height: 48px;
        }

        .platform-info {
            flex: 1;
        }

        .platform-name {
            font-weight: 600;
            font-size: 1.1em;
            margin-bottom: 0.25rem;
        }

        .platform-description {
            font-size: 0.9em;
            color: #7a7a7a;
        }

        .download-actions {
            display: flex;
            align-items: center;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .qr-code {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .installation-instructions {
            display: none;
            margin-top: 2rem;
            padding: 1.5rem;
        }

        .installation-instructions.visible {
            display: block;
        }

        .installation-instructions h3 {
            font-weight: 600;
            margin-bottom: 1rem;
            font-size: 1.1em;
        }

        .installation-instructions ol {
            margin-left: 2rem;
            margin-bottom: 1rem;
        }

        .installation-instructions li {
            margin-bottom: 0.5rem;
            line-height: 1.5;
        }

        .installation-instructions .note {
            font-size: 0.9em;
            color: #7a7a7a;
            font-style: italic;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #dbdbdb;
        }

        @media (max-width: 768px) {
            .platform-card {
                flex-direction: column;
                align-items: flex-start;
            }

            .download-actions {
                width: 100%;
                justify-content: space-between;
            }

            .download-actions .button {
                flex: 1;
            }

            .qr-code {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <section class="hero is-light">
        <div class="hero-body">
            <div class="container">
                <img src="/static/images/tamanu_logo.svg" alt="Tamanu Logo" style="height: 60px; margin-bottom: 2rem;" />
                <h1 class="title">Tamanu Mobile</h1>
                <p class="subtitle">Version {{ version.major }}.{{ version.minor }}.{{ version.patch }}</p>
            </div>
        </div>
    </section>

    <section class="section">
        <div class="container">
            <div class="content mb-5">
                <p>Download the Tamanu Mobile application to access patient information, capture data, and provide care even when offline.</p>
            </div>

            {% if artifacts %}
                {% for artifact in artifacts %}
                <div class="box">
                    <div class="platform-card">
                        <div class="platform-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="5" y="2" width="14" height="20" rx="2" ry="2" />
                                <line x1="12" y1="18" x2="12" y2="18" />
                            </svg>
                        </div>
                        <div class="platform-info">
                            {% if artifact.platform == "android" %}
                            <div class="platform-name">Android</div>
                            <div class="platform-description">For Android phones and tablets</div>
                            {% else %}
                            <div class="platform-name">{{ artifact.platform | capitalize }}</div>
                            <div class="platform-description">Mobile application</div>
                            {% endif %}
                        </div>
                        <div class="download-actions">
                            <a href="{{ artifact.download_url }}" class="button is-primary">Download</a>
                            <div class="qr-code">
                                {{ artifact.qr_code_svg | safe }}
                            </div>
                        </div>
                    </div>
                </div>
                {% endfor %}

                <div class="box installation-instructions" id="installation-instructions">
                    <h3>Installation Instructions</h3>
                    <ol>
                        <li>Open your <strong>Downloads</strong> folder</li>
                        <li>Tap the downloaded <strong>app-release.apk</strong> file</li>
                        <li>Tap <strong>"Install"</strong> when prompted</li>
                        <li>After installation completes, tap <strong>"Open"</strong></li>
                    </ol>
                    <p class="note">
                        If you encounter any issues, please contact your administrator for assistance.
                    </p>
                </div>

                <div class="box has-background-info-light">
                    <p><strong>Note:</strong> After downloading, you'll need to install the application manually on your device.</p>
                </div>
            {% else %}
                <div class="box">
                    <p class="has-text-centered has-text-grey-light">No mobile artifacts available for this version.</p>
                </div>
            {% endif %}

            <div class="mt-5">
                <a href="/" class="button is-light">← All versions</a>
            </div>
        </div>
    </section>

    <script>
        // Show installation instructions when download button is clicked
        for (const el of document.querySelectorAll('.button.is-primary')) {
            el.addEventListener('click', function (e) {
                setTimeout(() => {
                    document
                        .getElementById('installation-instructions')
                        .classList.add('visible');
                }, 500);
            });
        }
    </script>
</body>
</html>
