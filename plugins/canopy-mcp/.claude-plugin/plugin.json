{
	"name": "canopy-mcp",
	"version": "1.0.0",
	"description": "Fleet queries against Canopy: Tamanu servers, groups, versions, backups, incidents, and issues; read-only except support-recorded manual incidents (write-scoped tokens only). Requires a Canopy MCP bearer token credential for meta.tamanu.app."
}
